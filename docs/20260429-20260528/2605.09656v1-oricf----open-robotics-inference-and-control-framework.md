---
title: ORICF -- Open Robotics Inference and Control Framework
title_zh: ORICF -- 开放机器人推理与控制框架
authors: "Andrés Meseguer Valenzuela, Luís Miguel Bartolín Arnau"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09656v1"
tags: ["query:ur"]
score: 8.0
evidence: 开放机器人推理与控制框架，支持边缘卸载
tldr: "机器人部署AI模型面临高计算成本和能耗。本文提出ORICF，一个模块化、声明式、模型无关的框架，通过YAML配置灵活组合多模态推理管线，并支持边缘卸载。在移动机器人上的评估显示，相比机载执行，ORICF可将机器人端计算利用率降低83.16%，能耗降低65.8%。该框架在保持模块化和可重复性的同时，显著提升了部署效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09656v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 569, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.09656v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1639, \"height\": 444, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.09656v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1530, \"height\": 388, \"label\": \"Table\"}]"
motivation: 现有机器人AI部署计算昂贵、延迟高且能耗大，缺乏能灵活组合多模态推理管线的统一框架。
method: ORICF通过轻量YAML配置实现模型、硬件和数据通道的无代码修改切换，并支持将推理任务卸载到边缘计算机。
result: "在通过ASR、LLM和CNN检测器回答语音查询的任务中，ORICF边缘部署比机载执行降低计算利用率83.16%和能耗65.8%。"
conclusion: ORICF提供高效、灵活且可重复的机器人多模态推理部署方案，显著降低计算和能源开销。
---

## 摘要
人工智能（AI）的最新进展使机器人具备了有效的感知和语言模型，但其部署仍然计算开销大，增加了延迟和能耗。本文提出开放机器人推理与控制框架（ORICF），这是一个模块化、声明式且模型无关的平台，用于组合多模态机器人推理流水线。ORICF集成了输入/输出（I/O）适配器、可插拔推理后端和后处理逻辑，同时轻量级的YAML规范允许在无需修改代码的情况下更换模型、硬件目标和数据通道。该框架还支持边缘卸载，即在附近的外部计算机上执行推理，而非在机器人上。通过结合自动语音识别（ASR）、大型语言模型（LLM）和卷积神经网络（CNN）检测器，并通过机器人操作系统2（ROS2），ORICF在一台移动机器人上进行了评估，该机器人回答关于在其摄像头流中检测到的人的口头查询。与机载执行相比，基于ORICF的边缘部署将机器人侧的计算利用率降低了高达83.16%，估计能耗降低了65.8%，同时保持了模块化和可重复性。

## Abstract
Recent advances in artificial intelligence (AI) have enabled effective perception and language models for robots, but their deployment remains computationally expensive, increasing latency and energy use. This work presents the Open Robotics Inference and Control Framework (ORICF), a modular, declarative, and model-agnostic platform for composing multimodal robotic inference pipelines. ORICF integrates input/output (I/O) adapters, pluggable inference back ends, and post-processing logic, while lightweight YAML specifications allow models, hardware targets, and data channels to be changed without code modification. The framework also supports edge offloading, i.e., executing inference on nearby external computers instead of onboard the robot. ORICF is evaluated on a mobile robot that answers spoken queries about people detected in its camera stream by combining automatic speech recognition (ASR), a large language model (LLM), and a convolutional neural network (CNN) detector through Robot Operating System 2 (ROS2). Compared with onboard execution, ORICF-based edge deployment reduces robot-side compute utilization by up to 83.16% and estimated energy consumption by 65.8%, while preserving modularity and reproducibility.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前机器人部署 AI 模型（如感知、语言理解）面临高计算成本、高延迟和高能耗的问题。虽然已有模型优化运行时（如 ONNX Runtime）和模型库（如 Hugging Face），但缺乏统一、声明式且支持多模态推理流水线的框架，导致集成困难，且难以灵活切换模型、硬件和通信接口。
- **整体含义**：本文旨在通过一个模块化、模型无关且支持边缘卸载的开放框架（ORICF），降低机器人多模态 AI 部署的复杂度，提升部署效率，同时减轻机载计算负担、降低能耗，从而延长移动机器人的自主作业时间。

### 2. 论文提出的方法论：核心思想、关键技术细节（公式或算法流程用文字说明）

- **核心思想**：提出 ORICF——一个基于 Python 和 C++ 的开放式机器人推理与控制框架。采用“插件化”架构，将输入/输出（I/O）适配、推理后端和后处理逻辑解耦，通过轻量级 YAML 配置文件实现声明式部署，无需修改代码即可更换模型、硬件目标和数据通道。同时支持边缘卸载，将计算密集型推理任务转移到附近的外部计算机上执行。
- **关键技术细节**：
  - **三层模块**：I/O 模块（支持 ROS2 话题、RTSP 流、音频设备、文本通道等）、推理模块（提供统一后端接口，支持 ONNX Runtime、OpenVINO 等）、后处理模块（如目标计数、标签映射、图像标注）。
  - **多模态流水线**：通过 YAML 配置将不同模型（CNN、ASR、LLM）链接成流水线，中间结果通过 ROS2 话题或内部缓存传递。
  - **能量估计模型**：采用线性公式 \( P(u) = P_{\text{idle}} + u \cdot (P_{\text{full}} - P_{\text{idle}}) \)，其中 \( u \) 为处理器利用率，\( P_{\text{idle}} \) 和 \( P_{\text{full}} \) 分别为空闲和满载功耗（本文对 Jetson Orin NX 分别设为 5W 和 25W）。相对能耗降低比例 \( R = 1 - P_{\text{BASE}} / P_{\text{CNN+LLM}} \)。
- **算法流程（文字描述）**：
  1. 机器人接收语音命令 → 通过 ASR 转成文本。
  2. 同时，摄像头流通过 ROS2 话题传入，CNN（Person-detection-0200）检测人物，后处理模块统计人数并发布到 `/human_counter` 话题。
  3. LLM（TinyLlama-1.1B-Chat-v1.0）接收文本查询和检测结果，生成自然语言回答。
  4. ORICF 协调所有模块，可根据 YAML 配置选择在机载或边缘执行 CNN 和 LLM。

### 3. 实验设计：数据集/场景、基准、对比方法

- **场景**：人机交互——移动机器人通过自然语言回答“你看到多少人？”等口头查询。
- **数据集**：未使用公开数据集，而是使用实时摄像头流（Intel RealSense D455）和自由语音输入。
- **基准**：对比机载执行（全在机器人上运行）与 ORICF 边缘部署（CNN 和 LLM 卸载到边缘服务器）。
- **对比方法**：仅比较两种部署方式下的机器人侧计算利用率和估计能耗，未与现有其他框架（如 FogROS2、ROS-LLM）进行直接性能对比。

### 4. 资源与算力

- **机器人**：XMachines X100 移动机器人，搭载 **NVIDIA Jetson Orin NX（8GB RAM）**、Intel RealSense D455 RGB 摄像头。软件环境：Ubuntu 22.04 + ROS2 Humble。
- **边缘服务器**：Proxmox 虚拟化环境，12 核 CPU @2.1GHz，16GB RAM，同样 Ubuntu 22.04 + ROS2 Humble。
- **GPU 信息**：未明确提及 GPU 型号及数量，仅说明 Jetson Orin NX 为嵌入式平台（可集成 GPU），边缘服务器未说明是否有 GPU。
- **训练时长**：本论文不涉及模型训练，仅进行推理部署测试，未报告训练资源消耗。

### 5. 实验数量与充分性

- **实验数量**：单一实验场景（人机对话问答），在三种运行阶段（基线无推理、仅 CNN、CNN+LLM）下测量了机器人和边缘服务器的处理利用率（vmstat 采集），每组统计了平均值、中位数、方差、标准差。
- **充分性**：
  - **优点**：对比了机载与边缘两种部署方式的差异，数据粒度较细（多阶段测量）。
  - **不足**：
    - 未进行消融实验（如单独评估 ASR 或不同模型组合的影响）。
    - 未与手动集成的 ROS2 流水线对比，无法度量 ORICF 本身的框架开销。
    - 未考虑网络延迟、带宽波动、安全性、容错性等因素。
    - 仅一个任务场景，泛化性有限。
    - 能量估计基于理论模型，未实测实际功耗。

### 6. 论文的主要结论与发现

- **主要结论**：ORICF 作为一种模块化、声明式的编排层，能有效组织多模态推理流水线，并通过边缘卸载显著降低机器人上的计算负载和能耗。
- **具体发现**：
  - 机载运行 CNN+LLM 时，机器人中位数利用率达 95%，接近饱和；边缘卸载后机器人中位数利用率降至 16%，**降低 83.16%**。
  - 估计能耗从 24W 降至 8.2W，**降低 65.8%**。
  - 机载执行下计算利用率波动大（标准差 20%），边缘卸载波动减小，有利于实时性。

### 7. 优点：方法或实验设计上的亮点

- **框架设计**：
  - 模型无关性：支持 CNN、LLM、ASR 等多种模型类型，通过 YAML 配置即可切换，无需改代码。
  - 多模态编排：自然连接感知、语言、语音模块，中间结果解耦。
  - 边缘卸载透明化：ROS2 话题和 YAML 配置使得任务迁移到边缘服务器简单。
- **实验设计**：
  - 采用实际移动机器人 + 真实场景（人机对话），验证了框架在真实硬件上的可用性。
  - 对比了机载与边缘两种极端场景，量化了计算利用率和能耗优势。

### 8. 不足与局限

- **实验覆盖不全**：仅一个任务、一个机器人平台、一个边缘服务器配置，缺乏不同硬件、不同网络条件、不同任务复杂度的评估。
- **未评估框架开销**：没有与手动编写 ROS2 节点实现相同流水线的性能对比，因此无法确定 ORICF 本身的额外延迟或资源消耗。
- **能量估计粗糙**：基于利用率线性模型，未考虑处理器 DVFS 实际行为、外设功耗等因素，也未进行实际功率测量。
- **未考虑网络退化**：边缘卸载依赖于无线网络，实验中未讨论丢包、延迟抖动等问题。
- **安全性/容错性缺失**：文中明确提到未评估安全机制和故障恢复，这对于实际部署至关重要。
- **模型选择**：使用的 LLM（TinyLlama-1.1B）和 CNN 模型尺寸较小，若使用更大模型（如 Llama-7B、YOLOv8）结果可能不同。

（完）
