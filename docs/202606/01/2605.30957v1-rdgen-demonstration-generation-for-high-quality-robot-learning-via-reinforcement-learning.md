---
title: "RDGen: Demonstration Generation for High-Quality Robot Learning via Reinforcement Learning"
title_zh: "RDGen: 基于强化学习的高质量机器人学习演示生成"
authors: "Zijian Zhu, Menglin Zou, Zhuang Li, Yaojie Tu, Xinhai Sun"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.30957v1"
tags: ["query:ur"]
score: 8.0
evidence: 通过强化学习生成机器人演示数据，直接相关于机器人数据集构建
tldr: 针对VLA模型训练数据依赖人工遥操作成本高、难扩展的问题，提出RDGen框架，利用强化学习从仿真到真实生成高质量机器人演示。通过VLM解析任务、Grounding DINO定位对象，将RL策略作为轨迹生成器，收集成功轨迹用于下游训练。实验表明，RDGen产生的轨迹更平滑，下游VLA性能显著优于人工遥操作，表明RL生成的演示可作为更可靠的监督信号。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30957v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1345, \"height\": 675, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30957v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 858, \"height\": 472, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30957v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1541, \"height\": 189, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30957v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1294, \"height\": 164, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30957v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1213, \"height\": 271, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30957v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 820, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30957v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 510, \"height\": 269, \"label\": \"Table\"}]"
motivation: 现有VLA模型依赖昂贵且难规模化的人工遥操作数据，需要自动化的高质量演示生成方法。
method: 提出RDGen，结合VLM任务解析、Grounding DINO定位和sim-to-real RL策略，将RL成功轨迹作为高质量演示。
result: 在抓取任务上，RDGen轨迹更平滑，下游VLA性能超过人工遥操作，任务成功率较高。
conclusion: RL生成的演示可替代人工遥操作，为机器人策略学习提供更可靠一致的监督信号。
---

## 摘要
视觉-语言-动作（VLA）模型已成为通用机器人控制的一种有前景的范式。然而，其性能仍从根本上受限于高质量机器人轨迹数据的可用性。在当前的机器人学习实践中，此类数据主要通过人类远程操作收集，这种方式劳动密集、成本高昂且难以扩展。本文提出RDGen，一种用于生成高质量机器人演示的从仿真到现实的强化学习框架。RDGen并非仅将强化学习用作最终控制策略，而是利用训练好的RL策略作为结构化轨迹生成器。该系统包含一个基于VLM的任务解析器，用于识别任务相关物体；一个基于Grounding DINO的物体定位器；以及一个从仿真迁移到真实机器人的RL策略。成功运行的结果被收集为干净、高质量的演示，用于下游VLA训练，而仿真阶段进一步以极低的边际成本提供可扩展的额外轨迹来源。在拾取与放置任务上的实验表明，迁移后的RL策略实现了高任务成功率。与人类远程操作相比，RDGen生成的轨迹显著更平滑，并带来更优的下游VLA性能。这些结果表明，RL生成的演示可作为机器人策略学习更可靠且一致的监督信号。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for general-purpose robot control. However, their performance remains fundamentally constrained by the availability of high-quality robot trajectory data. In current robot learning practice, such data are primarily collected through human teleoperation, which is labor-intensive, costly, and difficult to scale. In this paper, we propose RDGen, a sim-to-real reinforcement learning framework for generating high-quality robot demonstrations. Rather than employing reinforcement learning solely as the final control policy, RDGen leverages trained RL policies as a structured trajectory generator. The system consists of a VLM-based task parser that identifies task-relevant objects, a Grounding DINO-based object localizer, and an RL policy transferred from simulation to the real robot. Successful rollouts are then harvested as clean, high-quality demonstrations for downstream VLA training, while the simulation stage further provides a scalable source of additional trajectories at little marginal cost. Experiments on a pick-and-place task demonstrate that the transferred RL policy achieves a high task success rate. Compared with human teleoperation, RDGen produces significantly smoother trajectories and yields superior downstream VLA performance. These results indicate that RL-generated demonstrations can serve as more reliable and consistent supervisory signals for robot policy learning.

---

## 论文详细总结（自动生成）

# RDGen: 基于强化学习的高质量机器人学习演示生成

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉-语言-动作（VLA）模型在通用机器人控制中表现出巨大潜力，但其性能严重受限于高质量机器人轨迹数据的可用性。当前主流的数据收集方式——人类远程操作——劳动密集、成本高昂且难以规模化扩展（例如需要大量人力、不同平台适配困难）。
- **研究动机**：探索一种自动化、可扩展的高质量轨迹生成方法，以替代人工遥操作，进而提升下游VLA策略的学习效果。
- **整体含义**：论文提出将强化学习（RL）从“最终部署策略”重新定位为“数据生成引擎”，通过仿真到现实（sim-to-real）的RL策略自动化产出成功、平滑、物理可执行的机器人演示，为VLA训练提供更可靠的监督信号。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- **RL作为数据生成器**：训练一个RL策略完成特定任务，然后收集其成功运行轨迹作为高质量演示，用于下游VLA模型的训练，而非将RL本身作为最终部署的控制器。
- **全流程自动化**：从自然语言指令到真实机器人执行与数据收集的端到端管道，结合视觉语言模型（VLM）任务解析、语言引导的物体定位、sim-to-real迁移策略。

### 关键技术细节
1. **任务理解与规划**：
   - 使用VLM（Qwen3-VL）作为智能体，接收语言指令`l`和当前观测`o_t`，维护结构化记忆`m_t`，规划下一个原子动作（技能类型、操作物体描述、目标区域描述）。
   - 动作表示为`a_t = (s_t, q_obj_t, q_tar_t)`，其中`s_t`为技能类型，`q_obj_t`和`q_tar_t`为物体或区域的自然语言描述。
   - 包含两个解耦角色：规划器（Planner）和验证器（Verifier），验证执行结果并更新记忆，支持重试或重新规划。

2. **物体定位**：
   - 使用Grounding DINO将语言查询`q`映射到RGB图像中的边界框，取最高置信度框`B_q`。
   - 结合对齐的深度图、相机内参和外参，将2D边界框提升为机器人坐标系下的3D位置`p_q^r`。
   - 将符号动作实例化为度量命令：`(s_t, p_obj^r, p_tar^r)`，作为低层RL策略的输入。

3. **低层RL策略学习**：
   - **观测空间**（14维）：目标位置`p_target∈R³`、末端执行器位置`p_ee∈R³`、末端执行器四元数`q_ee⁺`及其负表示`q_ee⁻`（共计4+4维）。
   - **动作空间**（6维）：末端执行器笛卡尔位置偏移和欧拉角增量`[Δx,Δy,Δz,Δϕ,Δθ,Δψ]`，通过逆运动学转换为关节命令。
   - **训练算法**：使用Soft Actor-Critic（SAC）在Isaac Sim中训练，最大熵目标：
     ```
     π* = argmax_π E[Σγ^t (r(s_t,a_t) + αH(π(·|s_t)))]
     ```
   - **奖励函数**：综合任务完成奖赏、进度奖赏（距离缩减）、中心奖赏（坐标方向）、路径偏差惩罚、静止惩罚、IK失败惩罚、碰撞惩罚等。
   - **重置策略**：提前终止无效状态（与桌子碰撞、目标移动、IK失败、超时），重设机器人并重新采样目标位置，提升训练效率。

4. **Sim-to-Real适应**：
   - 训练时对末端执行器位姿加入随机噪声（位置5mm、四元数0.1扰动）。
   - 采用**双四元数表示**：在观测中包含`(q_ee, -q_ee)`，并根据`w`的符号固定顺序，消除四元数符号歧义导致的观测不连续，使策略对真实传感器噪声更鲁棒。

5. **数据收集**：
   - 真实机器人上成功运行被保留为清洁演示。
   - 仿真环境也作为低成本数据源，通过改变光照、物体位姿、视角等生成多样化轨迹，用于预训练。

## 3. 实验设计：使用的数据集/场景、benchmark、对比方法

### 场景与任务
- **任务**：拾取与放置（pick-and-place）——抓取一个灰色立方体并放入纸盒；此外还测试了抓取可乐瓶放入纸盒。
- **平台**：Marvin M6CCS机械臂 + Ruiyan RY-H2灵巧手 + Gemini 435Le相机，仿真环境使用Isaac Sim构建相同配置。

### Benchmark与对比
- **对比方法**：人类远程操作（Teleoperation）收集的演示数据。
- **评估指标**：
  - **轨迹平滑度**：计算末端执行器笛卡尔平均加加速度（jerk，加速度的时间导数），数值越低越平滑。
  - **下游VLA性能**：使用收集的演示训练π0策略（基于流匹配的VLA模型），在10次随机初始化物体位置的试验中报告成功率。

### 数据规模
- 每种方法收集20条轨迹用于平滑度评估。
- VLA训练在10次试验上测试。

## 4. 资源与算力

- **GPU**：附录A.1表3列出使用NVIDIA RTX PRO 6000。
- **数量与时长**：论文未明确说明使用的GPU数量、训练时长或其他算力细节。

## 5. 实验数量与充分性

- **实验数量**：仅包含两个任务（立方体、可乐瓶），每个任务两种数据源对比。
- **消融实验**：无消融实验，没有分析各组件（如双四元数、路径惩罚）的单独贡献。
- **充分性评价**：
  - **优点**：平滑度和下游成功率两个指标直接呼应核心论点，实验设计简洁明确。
  - **不足**：任务单一（仅粗操作），场景变化有限；没有跨机器人、多技能验证；没有统计显著性检验或更多随机种子；VLA训练仅用20条轨迹，规模较小；没有与合成数据（如Domain Randomization生成的数据）对比。

## 6. 论文的主要结论与发现

- RDGen生成的轨迹显著比人类遥操作更平滑：立方体任务平均加加速度0.47 vs 2.68 m/s³，可乐瓶任务0.57 vs 5.59 m/s³。
- 下游VLA策略在使用RDGen数据后成功率更高：立方体80% vs 60%，可乐瓶100% vs 70%。
- 结论：RL生成的演示可作为比人类遥操作更可靠、更一致的监督信号，用于机器人策略学习。

## 7. 优点

- **自动化程度高**：从语言指令到真实执行的全流程自动化，大幅降低人力成本。
- **数据质量可控**：利用RL策略的自主优化特性，产生目标导向、物理可执行、时间一致的轨迹，且可通过奖励函数引导平滑性。
- **仿真扩展性**：仿真环境可低成本生成大量多样化轨迹，用于预训练或数据增广。
- **Sim-to-Real设计巧妙**：双四元数表示解决了四元数符号歧义的问题，增强策略在真实世界的鲁棒性。
- **与主流VLA范式兼容**：生成的轨迹可直接用于训练π0、OpenVLA等模型，具有实际应用价值。

## 8. 不足与局限

- **任务覆盖有限**：仅验证了抓取-放置这类粗操作，未涉及精细操作（如折衣服、拧螺丝），这些任务需要毫米级精度和更复杂的奖励函数设计。
- **任务特定设计**：部分训练技巧（如用弹簧机构随机化物体位置）是针对抓取任务定制的，不易直接迁移到其他技能。
- **训练管道未完全通用**：原子技能集需预先定义，新技能需要重新设计奖励和训练；VLM规划依赖于闭集技能，限制了开放性。
- **缺乏系统性消融和跨域验证**：未评估各组件贡献，未在不同机器人平台、不同场景（如杂乱桌面）上测试。
- **实验规模较小**：仅20条轨迹和10次试验，统计显著性不足；未报告随机种子和方差（除平滑度的标准差外）。
- **算力资源未充分说明**：GPU数量和训练时长未给出，影响可复现性评估。

（完）
