---
title: "Lightweight Transformer Models for On-Device Fault Detection: A Benchmark Study on Resource-Constrained Deployment"
title_zh: 面向设备端故障检测的轻量级Transformer模型：资源受限部署的基准研究
authors: Disha Patel
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24173v1"
tags: ["query:ur"]
score: 9.0
evidence: 在资源受限硬件上对设备端故障检测的轻量级Transformer模型进行基准测试；与人类机器人边缘部署直接相关
tldr: "针对资源受限设备上的在线故障检测，本文对比了传统机器学习（随机森林、XGBoost等）与轻量级Transformer（DistilBERT、TinyBERT等）在三个公开数据集上的性能。结果显示，在传感器数据良好分离时，轻量级Transformer可达到与传统方法相当的F1分数（87.8%），但模型大小和延迟分别高出100倍和9000倍。TinyBERT-4L在55MB和18ms延迟下表现最佳；INT8量化将模型缩小25%并保持精度。自适应两阶段管线（量化三模型分流97.9%预测）在19.5ms内达到87.6% F1。而在严重不平衡数据集上所有方法均表现不佳，揭示了当前方法在极端类不平衡故障检测中的局限性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24173v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 900, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24173v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 900, \"height\": 1156, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24173v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 909, \"height\": 541, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24173v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1608, \"height\": 818, \"label\": \"Table\"}]"
motivation: 在资源受限设备上部署故障检测模型需权衡精度、延迟与模型大小，现有轻量级Transformer的适用性尚不明确。
method: 采用NASA C-MAPSS、SECOM和UCI AI4I 2020数据集，对比传统ML（RF、XGBoost等）与轻量级Transformer（DistilBERT、TinyBERT、MobileBERT），并评估INT8量化与自适应两阶段推理管线。
result: "TinyBERT-4L在55MB和18ms延迟下达87.8% F1；量化后模型缩小25%且F1 86.9%；自适应管线达87.6% F1/19.5ms。不平衡数据上所有方法均欠佳。"
conclusion: 轻量级Transformer仅适用于传感器数据分离良好的场景，对极端不平衡数据仍缺乏有效方案；量化与自适应推理可缓解部署瓶颈。
---

## 摘要
设备端故障检测能够在无需依赖云的情况下实现实时诊断，但将机器学习模型部署在资源受限的硬件上需要在准确性、延迟和模型大小之间进行仔细权衡。我们提出了一个基准研究，比较传统机器学习方法（随机森林、XGBoost、支持向量机、逻辑回归）与轻量级Transformer架构（DistilBERT、TinyBERT-6L、TinyBERT-4L、MobileBERT）在三个公开数据集（NASA C-MAPSS涡扇退化、SECOM半导体制造、UCI AI4I 2020预测维护）上的二分类故障检测性能。我们评估了分类性能（F1分数、AUC）、模型大小和CPU推理延迟，并进一步评估了INT8动态量化和两阶段自适应推理流程。我们的结果表明，在分离良好的传感器数据（C-MAPSS）上，轻量级Transformer以87.8%的F1分数匹配传统机器学习，但模型大小高出100倍，延迟高出9000倍。TinyBERT-4L以55 MB和18毫秒CPU延迟成为最易于部署的Transformer。INT8量化将大小减少25%，同时保持86.9%的F1分数。我们的自适应流程通过量化的分流模型路由97.9%的预测，仅2.1%路由到更大的专家模型，在19.5毫秒平均延迟下实现了87.6%的F1分数。在严重不平衡的数据集（SECOM、UCI-PM）上，传统方法和Transformer方法都表现不佳，突显了当前方法在故障检测中极端类别不平衡情况下的基本局限性。所有代码均已公开。

## Abstract
On-device fault detection enables real-time diagnostics without cloud dependency, but deploying machine learning models on resource-constrained hardware demands careful tradeoffs between accuracy, latency, and model size. We present a benchmark comparing traditional ML methods (Random Forest, XGBoost, SVM, Logistic Regression) against lightweight transformer architectures (DistilBERT, TinyBERT-6L, TinyBERT-4L, MobileBERT) for binary fault detection across three public datasets: NASA C-MAPSS turbofan degradation, SECOM semiconductor manufacturing, and UCI AI4I 2020 predictive maintenance. We evaluate classification performance (F1-score, AUC), model size, and CPU inference latency, and further assess INT8 dynamic quantization and a two-stage adaptive inference pipeline. Our results reveal that on well-separated sensor data (C-MAPSS), lightweight transformers match traditional ML at 87.8% F1 but at 100x the model size and 9000x the latency. TinyBERT-4L emerges as the most deployment-friendly transformer at 55 MB and 18 ms CPU latency. INT8 quantization reduces size by 25% while preserving 86.9% F1. Our adaptive pipeline, routing 97.9% of predictions through a quantized triage model and only 2.1% to a larger expert, achieves 87.6% F1 at 19.5 ms average latency. On severely imbalanced datasets (SECOM, UCI-PM), both traditional and transformer methods struggle significantly, highlighting fundamental limitations of current approaches for extreme class imbalance in fault detection. All code is publicly available.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：设备端故障检测（on-device fault detection）旨在实现实时诊断，避免云依赖，但资源受限的硬件（如手机、IoT设备）对模型精度、延迟和大小提出了严苛的权衡要求。现有轻量级Transformer在自然语言处理（NLP）任务上表现优异，但它们在**表格传感器数据故障检测**场景下的适用性尚未被系统研究。
- **整体含义**：本文提供了一个诚实、可复现的基准，系统比较传统机器学习方法与轻量级Transformer在三个公开故障检测数据集上的性能、大小和延迟，揭示了Transformer在资源受限部署中的实际局限，并提出了缓解方案（INT8量化、自适应推理管线）。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将表格传感器数据序列化为“特征名:值”文本（如 “sensor1:0.23 sensor2:0.45 …”），利用预训练语言模型（BERT变体）的分类头进行二分类故障检测。同时引入模型压缩与自适应推理策略来改善部署效率。
- **关键技术细节**：
  - **传统基线**：Random Forest (RF-200)、XGBoost (200轮)、SVM (RBF核)、Logistic Regression (L2正则化)，均使用平衡类权重或scale_pos_weight处理不平衡。
  - **轻量级Transformer**：DistilBERT (66.9M参数)、TinyBERT-6L (67.0M)、TinyBERT-4L (14.3M)、MobileBERT (24.6M)。微调超参数：学习率2e-5~5e-5，batch size 32，warmup 100步，训练5~8 epoch，损失函数支持加权交叉熵。
  - **INT8动态量化**：对微调后的DistilBERT和TinyBERT-4L应用PyTorch `quantize_dynamic`，量化所有Linear层，无需重训练。
  - **自适应两阶段推理管线**：
    1. **Stage 1（分流模型）**：量化后的TinyBERT-4L (INT8) 对所有输入进行推理，若最大类概率 ≥ 阈值 τ（从{0.6, 0.65, ..., 0.95}中选出最优），则直接输出预测。
    2. **Stage 2（专家模型）**：置信度低于 τ 的样本转发给全精度DistilBERT重新处理。在C-MAPSS上，τ=0.7时，97.9%的样本由分流模型处理，仅2.1%需专家模型。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**（三个公开）：
  - **NASA C-MAPSS**：涡扇发动机退化仿真，24个特征，20,631样本，故障率15.0%（二分类：30周期内是否故障）。
  - **SECOM**：半导体制造，562个特征（经缺失值处理），1,567样本，故障率6.6%。
  - **UCI AI4I 2020 (UCI-PM)**：通用预测维护，8个特征（5传感器+3类型独热），10,000样本，故障率3.4%。
- **基准与对比方法**：
  - **传统ML**：RF-200、XGBoost、SVM、Logistic Regression。
  - **轻量级Transformer**：DistilBERT、TinyBERT-6L、TinyBERT-4L、MobileBERT。
  - **变体**：加权损失版本（后缀“w”）、INT8量化版本、自适应管线。
- **评价指标**：F1分数（主要）、AUC-ROC、模型大小（MB）、CPU推理延迟（ms，单样本平均50-100次，带预热）。
- **训练/测试分割**：80/20分层抽样，随机种子42，特征标准化基于训练集统计。

### 4. 资源与算力

- **训练硬件**：NVIDIA T4 GPU（单个，未明确数量）。
- **训练时长**：文中仅提到“fine-tuned for 5 epochs (initial run) and 7–8 epochs with weighted cross-entropy loss (fix run)”，但未给出具体训练时间（如小时数）。**未明确说明总算力消耗**。
- **推理测量**：CPU延迟在Google Colab CPU上测量（非专用移动硬件，如Apple Neural Engine或Qualcomm NPU）。

### 5. 实验数量与充分性

- **实验数量**：每个数据集上评估了约14种配置（4传统ML + 4标准Transformer + 4加权损失Transformer + 2量化 + 1自适应），加上MobileBERT失败记录，总计约45组有意义的实验结果。
- **消融与对比**：
  - 消融了加权损失对Transformer的影响。
  - 消融了INT8量化的精度-大小折中。
  - 消融了自适应管线的阈值选择（τ ∈ {0.6,0.65,…,0.95}）。
  - 提供了Pareto前沿分析（模型大小 vs. F1）。
- **充分性与客观性**：
  - 使用了公开数据集、固定随机种子、标准评估协议，避免cherry-pick。
  - 明确报告了MobileBERT训练失败（始终预测多数类），展现了诚实性。
  - **不足**：未进行剪枝（pruning）、量化感知训练（QAT）等更高级压缩技术；未在真实移动硬件（如ARM CPU、NPU）上测量延迟；对SECOM和UCI-PM仅做基本处理（未使用SMOTE等重采样方法）。总体实验设计合理且较全面，但深度上可再加强。

### 6. 论文的主要结论与发现

1. **在分离良好的数据上，Transformer匹配但不超越传统ML**：C-MAPSS上最佳Transformer (TinyBERT-6L) 与XGBoost均达87.9% F1，但XGBoost模型大小0.5 MB、延迟0.002 ms，而TinyBERT-6L为255 MB、133 ms（大小差510倍，延迟差66,500倍）。对干净表格数据，传统ML是实际更优选择。
2. **TinyBERT-4L是最适合部署的Transformer**：14.3M参数，55 MB，18 ms CPU延迟，F1 87.8%，比DistilBERT小4.6倍、快7.4倍。
3. **INT8量化有效降低大小且保持精度**：TinyBERT-4L+INT8大小41 MB（减少25%），F1 86.9%（仅降0.9%）。DistilBERT+INT8从255 MB降至132 MB（减少48%），F1降1.8%。
4. **自适应管线优化延迟与精度的权衡**：在C-MAPSS上，以19.5 ms平均延迟（比DistilBERT快7倍）达到87.6% F1，97.9%样本由轻量分流模型处理。
5. **极端类别不平衡击败所有方法**：在SECOM（6.6%故障）和UCI-PM（3.4%故障）上，最佳F1分别仅13.6%（Logistic Regression）和83.3%（XGBoost），Transformer完全无效（F1≤50%）。类别不平衡比模型架构更关键。
6. **MobileBERT不适用于表格数据**：在所有配置下预测全部为多数类，F=0%，归因于其倒瓶颈结构丢弃了数值细粒度信息。

### 7. 优点：方法或实验设计上的亮点

- **诚实透明的基准**：明确报告失败的模型（MobileBERT）和极低性能的结果，不刻意隐藏不理想数据。
- **系统全面的对比**：覆盖传统ML、多种轻量Transformer、量化压缩和自适应推理，提供多个维度（精度、大小、延迟）的Pareto分析。
- **实用导向的部署建议**：根据资源预算（<1 MB, 50-100 MB, >100 MB）给出具体模型推荐（XGBoost、TinyBERT-4L+INT8、自适应管线）。
- **失败分析**：单独分析MobileBERT失败原因及极端不平衡的困难，为后续研究提供方向。
- **代码开源**：提供GitHub仓库促进可复现研究。

### 8. 不足与局限

- **实验覆盖有限**：
  - 仅使用单一CPU（Colab）测量延迟，未在真实移动/NPU硬件上验证，数字可能不反映实际边缘设备表现。
  - 未评估其他压缩技术（如剪枝、知识蒸馏的极端版本、量化感知训练）。
  - 未尝试更先进的表格Transformer（如TabNet、FT-Transformer）或时间序列专用Transformer（如Informer）。
  - 表格到文本的序列化方式引入tokenization开销，可能不是最优表示（可尝试直接数值嵌入）。
- **不平衡处理不足**：仅用加权损失和平衡类权重，未应用SMOTE、Focal Loss或异常检测专用方法，导致在SECOM和UCI-PM上表现极差，结论有一定局限性。
- **数据集代表性**：C-MAPSS是仿真数据，特征分离好；SECOM特征维度高但样本少；UCI-PM特征维度低。真实工业场景可能更复杂（多类、缺失、噪声），当前结论泛化性需谨慎。
- **未讨论时间序列特性**：C-MAPSS本身是时间序列，但方法按静态表格处理，未利用LSTM或Transformer的序列建模能力（如位置编码、滑动窗口），这可能限制了Transformer的潜力。
- **透明性不足**：未提供训练收敛曲线、超参数搜索细节、置信度分布等。

（完）
