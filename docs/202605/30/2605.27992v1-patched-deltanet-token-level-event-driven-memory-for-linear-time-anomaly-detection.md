---
title: "Patched-DeltaNet: Token-Level Event-Driven Memory for Linear-Time Anomaly Detection"
title_zh: Patched-DeltaNet：面向线性时间异常检测的令牌级事件驱动记忆
authors: "Tae-Gyun Lee, Junyoung Park, Kyu Won Han"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27992v1"
tags: ["query:humanoid-ood"]
score: 8.0
evidence: 线性时间复杂度的轻量级异常检测架构，适合边缘部署
tldr: 时间序列异常检测中Transformer计算复杂度过高，限制实际部署。本文提出Patched-DeltaNet，通过时间序列分片与门控Delta网络结合，实现token级事件驱动记忆，仅在检测到显著物理变化时更新状态，有效过滤背景噪声并捕捉异常漂移。在SMD数据集上达到ROC-AUC 0.957和PA-F1 0.822，计算复杂度降至理论最小值O(L/P)。该方法兼具高性能与低资源消耗，适用于资源受限系统。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.27992v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1304, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27992v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1304, \"height\": 351, \"label\": \"Table\"}]"
motivation: 解决Transformer在时间序列异常检测中计算复杂度过高、难以部署于资源受限环境的问题。
method: 结合时间序列分片与门控Delta网络，设计token级事件驱动记忆机制，仅在显著物理变化时更新循环状态。
result: 在SMD基准上取得ROC-AUC 0.957和PA-F1 0.822，计算复杂度降为理论最小值O(L/P)。
conclusion: Patched-DeltaNet实现了线性时间复杂度的异常检测，兼顾高性能与低资源消耗，适用于资源受限系统。
---

## 摘要
时间序列异常检测对于维护任务关键型系统的可靠性至关重要。虽然基于Transformer的模型（如PatchTST）表现出色，但其$\mathcal{O}(L^2)$的计算复杂度严重限制了在资源受限环境中的部署。本文提出Patched-DeltaNet，一种结合时间序列分块与门控Delta网络的新型架构。通过整合这些范式，我们假设并证明了令牌级事件驱动记忆的涌现：分块机制提取局部语义块，而基于误差的Delta网络仅在发生显著物理变化（定义为增量）时更新其循环状态。这种协同作用有效地滤除了背景噪声并捕获了突发异常漂移。我们在服务器机器数据集（SMD）基准上的严格实验证明了Patched-DeltaNet的结构优越性和样本效率。通过在统一评估约束和相同计算预算下严格优于近期架构，我们的模型实现了0.957的ROC-AUC和0.822的PA-F1，同时将计算复杂度大幅降低至理论最小值$\mathcal{O}(L/P)$。

## Abstract
Time series anomaly detection is critical for maintaining the reliability of mission-critical systems. While Transformer-based models like PatchTST have shown remarkable performance, their $\mathcal{O}(L^2)$ computational complexity severely limits deployment in resource-constrained environments. In this paper, we propose Patched-DeltaNet, a novel architecture combining time-series patching with Gated Delta Networks. By integrating these paradigms, we hypothesize and demonstrate the emergence of token-level event-driven memory, whereby the patching mechanism extracts local semantic chunks, while the error-driven DeltaNet updates its recurrent state exclusively when significant physical changes, defined as deltas, occur. This synergy effectively filters out background noise and captures sudden anomalous drifts. Our rigorous experiments on the Server Machine Dataset (SMD) benchmark demonstrate the structural superiority and sample efficiency of Patched-DeltaNet. By strictly outperforming recent architectures under unified evaluation constraints and identical compute budgets, our model yields an ROC-AUC of 0.957 and PA-F1 of 0.822, while drastically reducing computational complexity to the theoretical minimum of $\mathcal{O}(L/P)$.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：时间序列异常检测对工业系统可靠性至关重要，但基于Transformer的模型（如PatchTST）因 $\mathcal{O}(L^2)$ 的计算复杂度，难以部署在资源受限的边缘设备上。
- **背景**：传统方法依赖离散错误码易遗漏无声故障；深度学习重建方法成为主流。线性时间模型（如Mamba、DeltaNet）虽效率高，但逐点处理易受噪声干扰，且序列长度缩减不理想。
- **目标**：提出一种同时保持高效（线性复杂度）与强异常判别能力的架构，适合实时、边缘场景。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：将时间序列分块（patching）与门控Delta网络结合，实现**令牌级事件驱动记忆**—仅在检测到显著物理变化（增量 $ \Delta_t \neq 0$）时更新循环状态，自动过滤背景噪声。
- **技术架构**：
  - **时间序列分块**：将输入 $X \in \mathbb{R}^{L \times F}$ 划分为长度为 $P$ 的非重叠块，得到 $N = \lfloor L/P \rfloor$ 个令牌，每个令牌扁平化为 $P \cdot F$ 维向量。这降低了有效序列长度从 $L$ 到 $N$。
  - **Gated DeltaNet 核心**：
    - 对每个令牌 $x_t$ 投影得到查询 $q_t$、键 $k_t$、值 $v_t$。
    - 计算误差驱动增量：$\Delta_t = v_t - S_{t-1} k_t$。
    - 生成数据依赖的门控向量：$\beta_t = \sigma(W_\beta x_t + b_\beta) \in \mathbb{R}^{d_k}$。
    - 更新记忆状态：$S_t = \text{diag}(\beta_t) S_{t-1} + \Delta_t k_t^\top$。
    - 输出：$o_t = S_t q_t$。
  - **异常评分**：将输出投影回原维度，用MSE重建误差作为异常分数。

#### 3. 实验设计
- **数据集**：仅使用**Server Machine Dataset (SMD)**，一个38特征的多元时间序列基准。
- **Benchmark**：在SMD上评估ROC-AUC和Point-Adjusted F1-Score (PA-F1)。
- **对比方法**：
  - **PatchTST**：用标准自注意力替换DeltaNet核心，其余分块设置相同。
  - **Reverso**：点式DeltaNet模型（设置 $P=1$ 无分块）。
- **训练配置**：滑动窗口长度 $L=100$，分块大小 $P=10$，隐藏维度 $d_{\text{model}}=128$；优化器Adam，MSE损失。
- **效率测试**：在 $L=8k \sim 512k$ 范围内测试延迟和显存（NVIDIA RTX 5080，bfloat16，批大小16）。

#### 4. 资源与算力
- **硬件**：单张 NVIDIA RTX 5080 GPU。
- **精度与批大小**：bfloat16，批大小16（效率测试）；训练细节（epoch数、学习率）未明确说明。
- **训练时长**：文中未报告。仅知使用Adam优化器和MSE损失。

#### 5. 实验数量与充分性
- **主要实验**：
  - 表1：三个模型在SMD上的性能（ROC-AUC、PA-F1、参数量、延迟）和复杂度对比。
  - 表2：极端长度（8K~512K）下的延迟和显存可扩展性测试。
  - 消融实验：移除门控机制后ROC-AUC从0.957降至0.949（PA-F1几乎不变）。
- **充分性评价**：
  - **优点**：统一实验协议，直接复现基线，对比公平；覆盖了性能、效率、消融三方面。
  - **不足**：
    - 仅使用单个数据集（SMD），泛化性存疑。
    - 未与Mamba、Anomaly Transformer、USAD等流行方法比较。
    - 未报告多次重复运行或统计显著性（如标准差）。
    - 训练超参数（学习率、epoch数、验证策略）未给出，影响可复现性。

#### 6. 主要结论与发现
- Patched-DeltaNet在SMD上达到 **ROC-AUC 0.957** 和 **PA-F1 0.822**，严格优于复现的PatchTST和Reverso。
- 计算复杂度降至理论最小值 $\mathcal{O}(L/P)$（实际为线性）。在 $L=512k$ 时，延迟仅16.83ms（优于PatchTST的208ms和Reverso的4090ms），显存仅3.7GB。
- 门控机制有效衰减背景噪声，提升判别力（消融实验证实）。
- 分块提供了局部语义上下文，解决了点式处理的噪声敏感问题。

#### 7. 优点
- **架构创新**：巧妙结合分块与误差驱动线性注意力，实现“事件驱动”记忆，具理论简洁性。
- **效率与性能双优**：在保持线性复杂度的同时，判别力超越Transformer基线，适合边缘部署。
- **严格实验设计**：统一复现基线、相同计算预算，对比公正；可扩展性测试覆盖极端长度。
- **显式门控**：数据依赖的门控机制可解释性强，有助于理解过滤原理。

#### 8. 不足与局限
- **实验覆盖不足**：仅一个数据集（SMD），缺乏不同领域（如KPI、SWaT、MSL）的验证；未对比更多主流方法（如Mamba、AnomalyTransformer）。
- **偏差风险**：未报告多次运行统计，单次结果可能存在波动；消融实验中PA-F1几乎不变，可能因阈值敏感性掩盖了门控的真实贡献。
- **应用限制**：分块大小 $P$ 是超参数，对慢速漂移或极短时间尺度异常可能不够敏感；事件驱动假设依赖物理信号显著变化，对渐变型异常可能失效。
- **训练细节缺失**：未公开epoch数、学习率、训练时长，影响可复现性；代码是否开源未说明。

（完）
